# CINQFLOW Wave 0 — Backend Dependencies
# Python 3.14+ compatible (uses pre-built wheels)

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.35
alembic>=1.13.0
psycopg[binary]>=3.3.0      # psycopg3 — has Python 3.14 wheels

# Pydantic (use system-installed version)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Task queue
celery>=5.4.0
redis>=5.1.0

# HTTP client
httpx>=0.27.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
factory-boy>=3.3.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=24.1.0
croniter>=2.0.0